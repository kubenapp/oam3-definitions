# Code generated by KubeVela templates. DO NOT EDIT. Please edit the original cue file.
apiVersion: core.oam.dev/v1beta1
kind: TraitDefinition
metadata:
  annotations:
    definition.oam.dev/description: Add security context on K8s pod for your workload which follows the pod spec in path 'spec.template.'
  labels:
    custom.definition.oam.dev/ui-hidden: "true"
  name: security-context
spec:
  appliesToWorkloads:
    - '*'
  podDisruptive: true
  schematic:
    cue:
      template: |
        patch: spec: template: spec: securityContext: {
        	if parameter.runAsUser != _|_ {
        		runAsUser: parameter.runAsUser
        	}
        	if parameter.runAsGroup != _|_ {
        		runAsGroup: parameter.runAsGroup
        	}
        	if parameter.fsGroup != _|_ {
        		fsGroup: parameter.fsGroup
        	}
        }
        parameter: {
            // +usage=Specify user ID to run the processes with.
        	runAsUser?:  number
            // +usage=Specify primary group ID to run the processes with.
        	runAsGroup?: number
            // +usage=Specify the owner group ID for volumes
        	fsGroup?:    number
        }

